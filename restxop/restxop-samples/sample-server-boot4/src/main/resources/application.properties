# restxop needs the servlet multipart machinery off multipart/related
# requests; this service takes no form uploads, so disable it entirely
# (see the restxop README resolver pitfall)
spring.servlet.multipart.enabled=false

server.port=8080
